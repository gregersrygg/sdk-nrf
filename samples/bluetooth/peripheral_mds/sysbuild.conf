# Build MCUboot as the bootloader. Required by CONFIG_NCS_SAMPLE_MCUMGR_BT_OTA_DFU
# (depends on BOOTLOADER_MCUBOOT), which pulls in the MCUmgr SMP Bluetooth
# transport used for OTA DFU and the echo ("Hello!") test in nRF Connect
# Device Manager.
SB_CONFIG_BOOTLOADER_MCUBOOT=y
